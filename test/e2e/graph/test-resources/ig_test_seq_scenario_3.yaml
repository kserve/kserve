apiVersion: serving.kserve.io/v1alpha1
kind: InferenceGraph
metadata:
  name: sequence-graph
spec:
  nodes:
    root:
      routerType: Sequence
      steps:
      - serviceName: {{ error_404_isvc_id }}
        dependency: Hard
      - serviceName: {{ success_200_isvc_id }}
        dependency: Soft