apiVersion: v1
kind: Service
metadata:
  name: seaweedfs-tls-serving-service
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: seaweedfs-tls-serving
spec:
  ports:
  - name: https
    port: 8333
    protocol: TCP
    targetPort: 8333
  selector:
    app: seaweedfs-tls-serving