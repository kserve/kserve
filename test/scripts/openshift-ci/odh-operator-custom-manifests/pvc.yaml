apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kserve-custom-manifests
  namespace: openshift-operators
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
volumeMode: Filesystem
