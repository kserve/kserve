apiVersion: v1
kind: Service
metadata:
  name: {{ include "kserve-resources.fullname" . }}-llmisvc-webhook-svc
  labels:
  {{- include "kserve-resources.labels" . | nindent 4 }}
spec:
  type: {{ .Values.llmisvcWebhookSvc.type }}
  selector:
    control-plane: llmisvc-controller-manager
    {{- include "kserve-resources.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.llmisvcWebhookSvc.ports | toYaml | nindent 2 }}
