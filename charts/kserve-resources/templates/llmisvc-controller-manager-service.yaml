apiVersion: v1
kind: Service
metadata:
  name: {{ include "kserve-resources.fullname" . }}-llmisvc-controller-manager-service
  labels:
    app.kubernetes.io/component: controller
    control-plane: llmisvc-controller-manager
    controller-tools.k8s.io: "1.0"
  {{- include "kserve-resources.labels" . | nindent 4 }}
spec:
  type: {{ .Values.llmisvcControllerManagerService.type }}
  selector:
    control-plane: llmisvc-controller-manager
    controller-tools.k8s.io: "1.0"
    {{- include "kserve-resources.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.llmisvcControllerManagerService.ports | toYaml | nindent 2 }}
