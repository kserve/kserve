[project]
name = "error_404_isvc"
version = "0.15.1"
description = "Custom isvc that always returns 404 with a JSON message"
authors = ["The KServe Authors"]
license = "Apache-2.0"
packages = [
    { include = "*.py" }
]
