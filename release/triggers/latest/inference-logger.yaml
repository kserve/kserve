---
### Specify as necessary ###
name: inference-inference-inferencelogger-latest
description: inference-inference-inferencelogger-latest
substitutions:
  _COMPONENT: "inference-inference-inferencelogger"
  _DOCKERFILE: "./inference-inference-inferencelogger.Dockerfile"
  _CONTEXT: "."
###########################
github:
  owner: kubeflow
  name: kfserving
  push:
    branch: master
###########################
build:
  steps:
  - name: docker
    args:
    - "build"
    - "-t"
    - "gcr.io/$REPO_NAME/${_COMPONENT}:latest"
    - "-f"
    - "${_DOCKERFILE}"
    - "${_CONTEXT}"
  images:
  - gcr.io/$REPO_NAME/${_COMPONENT}:latest
