---
### Specify as necessary ###
name: inference-inference-inferencelogger-tagged
description: inference-inference-inferencelogger-tagged
substitutions:
  _COMPONENT: "inference-inference-inferencelogger"
  _DOCKERFILE: "./inference-inference-inferencelogger.Dockerfile"
  _CONTEXT: "."
###########################
github:
  owner: kubeflow
  name: kfserving
  push:
    tag: v*.*.*
###########################
build:
  steps:
  - name: docker
    args:
    - "build"
    - "-t"
    - "gcr.io/$REPO_NAME/${_COMPONENT}:$TAG_NAME"
    - "-f"
    - "${_DOCKERFILE}"
    - "${_CONTEXT}"
  images:
  - gcr.io/$REPO_NAME/${_COMPONENT}:$TAG_NAME
