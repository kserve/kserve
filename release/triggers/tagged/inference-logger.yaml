---
### Specify as necessary ###
name: inference-inference-logger-tagged
description: inference-inference-logger-tagged
substitutions:
  _COMPONENT: "inference-inference-logger"
  _DOCKERFILE: "./inference-inference-logger.Dockerfile"
  _CONTEXT: "."
###########################
github:
  owner: kubeflow
  name: kfserving
  push:
    tag: v*.*.*
###########################
build:
  steps:
  - name: docker
    args:
    - "build"
    - "-t"
    - "gcr.io/$REPO_NAME/${_COMPONENT}:$TAG_NAME"
    - "-f"
    - "${_DOCKERFILE}"
    - "${_CONTEXT}"
  images:
  - gcr.io/$REPO_NAME/${_COMPONENT}:$TAG_NAME
