---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: kernelcachecaptures.serving.kserve.io
spec:
  group: serving.kserve.io
  names:
    kind: KernelCacheCapture
    listKind: KernelCacheCaptureList
    plural: kernelcachecaptures
    shortNames:
    - kcc
    singular: kernelcachecapture
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.targetImage
      name: Image
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              cacheFramework:
                enum:
                - vllm
                - tgi
                - triton-python
                - gaudi
                type: string
              cachePathOverride:
                type: string
              targetImage:
                type: string
            required:
            - targetImage
            type: object
            x-kubernetes-validations:
            - message: at least one of cacheFramework or cachePathOverride must be
                set
              rule: has(self.cacheFramework) || has(self.cachePathOverride)
            - message: cacheFramework and cachePathOverride are mutually exclusive;
                set one or the other
              rule: '!(has(self.cacheFramework) && has(self.cachePathOverride))'
          status:
            properties:
              capturedAt:
                format: date-time
                type: string
              capturedCacheSizeBytes:
                format: int64
                type: integer
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              detectedCachePath:
                type: string
              imageDigest:
                type: string
              kernelCacheRef:
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                type: object
              phase:
                type: string
              podName:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
