apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: kserve-kernelcachenode-agent
spec:
  template:
    spec:
      nodeSelector:
        amd.com/gpu: "true"
      tolerations:
        - key: amd.com/gpu
          operator: Exists
          effect: NoSchedule
