apiVersion: v1
kind: Service
metadata:
  name: kernelcache-webhook-server-service
  namespace: kserve
spec:
  ports:
    - port: 443
      targetPort: webhook-server
  selector:
    control-plane: kserve-kernelcache-controller-manager
