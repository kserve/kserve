apiVersion: v1
kind: Service
metadata:
  name: models-web-app
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app.kubernetes.io/component: models-web-app
  type: ClusterIP
