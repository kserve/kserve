apiVersion: serving.kubeflow.org/v1beta1
kind: InferenceService
metadata:
  name: "torchserve-bert"
spec:
  predictor:
    pytorch:
      protocolVersion: v2
      storageUri: gs://kfserving-examples/models/torchserve/bertseq
      # storageUri: pvc://model-pv-claim
