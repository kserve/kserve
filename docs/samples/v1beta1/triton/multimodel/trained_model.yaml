apiVersion: "serving.kubeflow.org/v1alpha1"
kind: "TrainedModel"
metadata:
  name: "cifar10"
spec:
  inferenceService: bert
  model:
    framework: pytorch
    storageUri: s3://triton/torchscript/cifar10
