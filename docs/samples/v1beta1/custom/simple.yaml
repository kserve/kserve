apiVersion: "serving.kubeflow.org/v1beta1"
kind: "InferenceService"
metadata:
  name: "custom-simple"
spec:
  predictor:
    spec:
      containers:
      - image: codait/max-object-detector
        ports:
          - containerPort: 5000
