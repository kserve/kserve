apiVersion: serving.kubeflow.org/v1alpha2
kind: InferenceService
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: kfservingsdksample
spec:
  default:
    predictor:
      custom:
        container:
          image: {username}/kfservingsdksample
