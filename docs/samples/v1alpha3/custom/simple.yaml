apiVersion: "serving.kubeflow.org/v1alpha3"
kind: "Service"
metadata:
  name: "custom-simple"
spec:
  predictor:
    containers:
      - image: codait/max-object-detector
        ports:
          - containerPort: 5000