apiVersion: "serving.kubeflow.org/v1alpha2"
kind: "KFService"
metadata:
  name: "sklearn-iris"
spec:
  default:
    predictor:
      sklearn:
        modelUri: "gs://kfserving-samples/models/sklearn/iris"
