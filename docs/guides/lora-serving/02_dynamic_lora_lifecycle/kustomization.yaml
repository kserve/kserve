apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: REPLACE_ME_NAMESPACE

resources:
  # Core serving infrastructure
  - llm-inference-service-lora.yaml
  - httproute.yaml

  # Security: restrict LoRA management to in-cluster users
  - network-policy.yaml
  - rbac.yaml

  # Apply one of these to dynamically manage adapters:
  # - lora-load-job.yaml
  # - lora-unload-job.yaml
