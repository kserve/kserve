name: Transformer Docker Publisher

on:
  push:
    # Publish `master` as Docker `latest` image.
    branches:
      - master

  # Run tests for any PRs.
  pull_request:

jobs:
  # Run tests.
  # See also https://docs.docker.com/docker-hub/builds/automated-testing/
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run tests
        run: |
          if [ -f docker-compose.test.yml ]; then
            docker-compose --file docker-compose.test.yml build
            docker-compose --file docker-compose.test.yml run sut
          else
            cd python
            docker buildx build . --file custom_transformer.Dockerfile
          fi

  # Push image to GitHub Packages.
  # See also https://docs.docker.com/docker-hub/builds/
  push:
    # Ensure test job passes before pushing image.
    needs: test

    if: github.event_name == 'push'
    uses: ./.github/workflows/build-and-push.yml
    with:
      image-name: image-transformer
      context: python
      dockerfile-path: custom_transformer.Dockerfile
      platforms: linux/amd64
    secrets: inherit

  # Run tests.
  # See also https://docs.docker.com/docker-hub/builds/automated-testing/
  test-transformer-grpc:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run tests
        run: |
          if [ -f docker-compose.test.yml ]; then
            docker-compose --file docker-compose.test.yml build
            docker-compose --file docker-compose.test.yml run sut
          else
            cd python
            docker buildx build . --file custom_transformer_grpc.Dockerfile
          fi

  # Push image to GitHub Packages.
  # See also https://docs.docker.com/docker-hub/builds/
  push-transformer-grpc:
    # Ensure test job passes before pushing image.
    needs: test

    if: github.event_name == 'push'
    uses: ./.github/workflows/build-and-push.yml
    with:
      image-name: custom-image-transformer-grpc
      context: python
      dockerfile-path: custom_transformer_grpc.Dockerfile
      platforms: linux/amd64
    secrets: inherit
